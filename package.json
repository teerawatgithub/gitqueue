{
  "name": "queue",
  "version": "1.0.0",
  "description": "// 0. - install jest -> terminal -> npm install --save-dev jest //    - npx jest  //    - ดูผลลัพธ์ // 1. สร้าง test case ตาม comment ทั้งหมดใน Queue.test.js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^23.6.0"
  }
}
